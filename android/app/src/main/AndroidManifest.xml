<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- BLE permissions -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />

    <!-- Location permission (required for BLE scanning) -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Network -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-feature android:name="android.hardware.usb.host" android:required="false" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission
        android:name="android.permission.NEARBY_WIFI_DEVICES"
        android:usesPermissionFlags="neverForLocation" />

    <application
        android:name=".PlaudTemplateApp"
        android:networkSecurityConfig="@xml/network_security_config"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="${applicationLabel}"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PlaudTemplate"
        tools:targetApi="31">

        <!-- Onboarding -->
        <activity
            android:name=".ui.onboarding.WelcomeActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.onboarding.ScanningActivity"
            android:exported="false" />

        <activity
            android:name=".ui.onboarding.OnboardingSuccessActivity"
            android:exported="false" />

        <!-- Main -->
        <activity
            android:name=".ui.main.MainActivity"
            android:exported="false"
            android:launchMode="singleTask" />

        <!-- Recording (dark translucent theme) -->
        <activity
            android:name=".ui.recording.RecordingActivity"
            android:exported="false"
            android:theme="@style/Theme.PlaudTemplate.Recording" />

        <!-- Device Panel -->
        <activity
            android:name=".ui.home.DevicePanelActivity"
            android:exported="false" />

        <!-- File Detail -->
        <activity
            android:name=".ui.filedetail.FileDetailActivity"
            android:exported="false" />

        <activity
            android:name=".ui.settings.IntegrationSettingsActivity"
            android:exported="false" />
        <activity android:name=".ui.devices.DeviceSourcesActivity" android:exported="false" />
        <activity android:name=".ui.devices.PlaudOfflineSetupActivity" android:exported="false" />
        <activity android:name=".ui.devices.DingTalkA1Activity" android:exported="false" />
        <activity android:name=".ui.devices.FeishuRecorderActivity" android:exported="false" />

        <service
            android:name=".hub.service.BleSyncService"
            android:exported="false"
            android:foregroundServiceType="connectedDevice" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>
