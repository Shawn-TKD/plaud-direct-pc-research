<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bg_primary"
    android:orientation="vertical"
    android:paddingTop="16dp">

    <TextView
        android:layout_width="match_parent" android:layout_height="wrap_content"
        android:paddingStart="24dp" android:paddingEnd="24dp"
        android:fontFamily="sans-serif-light" android:text="Ask"
        android:textColor="@color/black" android:textSize="44sp" />

    <TextView
        android:id="@+id/libraryStatus"
        android:layout_width="match_parent" android:layout_height="wrap_content"
        android:paddingStart="26dp" android:paddingTop="2dp" android:paddingEnd="24dp"
        android:fontFamily="sans-serif" android:text="问你的全部录音"
        android:textColor="@color/text_secondary" android:textSize="14sp" />

    <HorizontalScrollView
        android:layout_width="match_parent" android:layout_height="wrap_content"
        android:layout_marginTop="18dp" android:clipToPadding="false"
        android:paddingStart="24dp" android:paddingEnd="24dp" android:scrollbars="none">
        <LinearLayout
            android:layout_width="wrap_content" android:layout_height="wrap_content"
            android:orientation="horizontal">
            <TextView android:id="@+id/promptSummary" style="@style/AskQuickAction" android:text="总结最近录音" />
            <TextView android:id="@+id/promptActions" style="@style/AskQuickAction" android:text="找出待办事项" />
            <TextView android:id="@+id/promptRecall" style="@style/AskQuickAction" android:text="我最近提到什么？" />
        </LinearLayout>
    </HorizontalScrollView>

    <ScrollView
        android:id="@+id/messagesScroll"
        android:layout_width="match_parent" android:layout_height="0dp" android:layout_weight="1"
        android:clipToPadding="false" android:fillViewport="true"
        android:paddingStart="24dp" android:paddingTop="20dp"
        android:paddingEnd="24dp" android:paddingBottom="16dp">
        <LinearLayout
            android:id="@+id/messagesContainer"
            android:layout_width="match_parent" android:layout_height="wrap_content"
            android:gravity="top" android:orientation="vertical" />
    </ScrollView>

    <LinearLayout
        android:layout_width="match_parent" android:layout_height="56dp"
        android:layout_marginStart="18dp" android:layout_marginEnd="18dp"
        android:layout_marginBottom="98dp" android:background="@drawable/bg_ask_input"
        android:elevation="2dp" android:gravity="center_vertical"
        android:orientation="horizontal" android:paddingStart="16dp" android:paddingEnd="6dp">
        <EditText
            android:id="@+id/questionInput"
            android:layout_width="0dp" android:layout_height="match_parent" android:layout_weight="1"
            android:background="@android:color/transparent" android:fontFamily="sans-serif"
            android:hint="问问你的录音…" android:imeOptions="actionSend"
            android:inputType="textCapSentences|textMultiLine" android:maxLines="3"
            android:paddingTop="0dp" android:paddingBottom="0dp"
            android:textColor="@color/text_primary" android:textColorHint="@color/text_secondary"
            android:textSize="15sp" />
        <ProgressBar
            android:id="@+id/askProgress"
            android:layout_width="40dp" android:layout_height="40dp"
            android:indeterminateTint="@color/black" android:padding="8dp" android:visibility="gone" />
        <ImageButton
            android:id="@+id/sendButton"
            android:layout_width="44dp" android:layout_height="44dp"
            android:background="@drawable/bg_play_button" android:contentDescription="发送"
            android:padding="10dp" android:src="@drawable/ic_send" />
    </LinearLayout>
</LinearLayout>
